# Copia este archivo a `.env` y completa los valores (`.env` no se sube a Git).
DJANGO_SECRET_KEY=cambia-esto-por-una-clave-larga-y-aleatoria
POSTGRES_DB=ros_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
